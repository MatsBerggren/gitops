apiVersion: v1
kind: Service
metadata:
  name: pve-exporter
  namespace: monitor
  labels:
    app: pve-exporter
spec:
  selector:
    app: pve-exporter
  ports:
    - name: http
      port: 9221
      targetPort: http
