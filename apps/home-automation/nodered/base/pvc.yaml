apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nodered-data
  namespace: nodered
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-rbd
  resources:
    requests:
      storage: 5Gi
