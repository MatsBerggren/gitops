
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: esphome

resources:
  - deployment.yaml
  - service.yaml
  - pvc.yaml
labels:
  - pairs:
      app: esphome
    includeSelectors: true
