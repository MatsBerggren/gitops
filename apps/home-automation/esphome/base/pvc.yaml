
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: esphome-config
  labels:
    app: esphome
spec:
  accessModes: [ ReadWriteOnce ]
  storageClassName: longhorn
  resources:
    requests:
      storage: 512Mi
