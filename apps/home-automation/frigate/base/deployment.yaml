apiVersion: apps/v1
kind: Deployment
metadata:
  name: frigate
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frigate
  template:
    metadata:
      labels:
        app: frigate
    spec:
      containers:
        - name: frigate
          image: ghcr.io/blakeblackshear/frigate:stable
          imagePullPolicy: IfNotPresent
          securityContext:
            privileged: true  # Required for GPU access
          ports:
            - containerPort: 5000  # UI/API
            - containerPort: 8554  # RTSP restream (optional)
            - containerPort: 8555  # WebRTC (optional)
          env:
            - name: FRIGATE_CONFIG_FILE
              value: /config/config.yaml
            - name: LIBVA_DRIVER_NAME
              value: i965  # Intel Quick Sync driver
          volumeMounts:
            - name: config
              mountPath: /config
            - name: db
              mountPath: /db
            - name: media
              mountPath: /media/frigate
            - name: shm
              mountPath: /dev/shm
            - name: dri
              mountPath: /dev/dri  # Intel GPU device
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "2000m"
              memory: "4Gi"
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: frigate-config
        - name: db
          persistentVolumeClaim:
            claimName: frigate-db
        - name: media
          persistentVolumeClaim:
            claimName: frigate-media
        - name: shm
          emptyDir:
            medium: Memory
            sizeLimit: 512Mi
        - name: dri
          hostPath:
            path: /dev/dri  # Intel GPU device for hardware acceleration
