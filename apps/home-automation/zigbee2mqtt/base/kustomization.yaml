
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: zigbee2mqtt

resources:
  - deployment.yaml
  - service.yaml
  - pvc.yaml
  - configmap.yaml
  
labels:
  - pairs:
      app: zigbee2mqtt
    includeSelectors: true
