apiVersion: v1
kind: Service
metadata:
  name: openbao
  namespace: openbao
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: openbao
  ports:
    - name: http
      port: 8200
      targetPort: 8200
