apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: velero

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - rbac.yaml
# cloud-credentials is provided by ExternalSecrets/OpenBao (see overlays/homelab)
#  - credentials-secret.yaml
  - backupstoragelocation.yaml
  - deployment.yaml

# Velero CRDs are managed by the separate app: apps/system-tools/velero-crds
