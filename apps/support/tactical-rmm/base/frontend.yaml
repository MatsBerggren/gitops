apiVersion: apps/v1
kind: Deployment
metadata:
  name: trmm-frontend
spec:
  replicas: 1
  selector:
    matchLabels: { app: trmm-frontend }
  template:
    metadata:
      labels: { app: trmm-frontend }
    spec:
      containers:
      - name: frontend
        image: docker.io/tacticalrmm/tactical-frontend:1.2.0
        env:
        - name: API_HOST
          valueFrom: { configMapKeyRef: { name: trmm-config, key: API_HOST } }
        volumeMounts:
        - { name: trmm-data, mountPath: /opt/tactical }
      volumes:
      - name: trmm-data
        persistentVolumeClaim: { claimName: trmm-data-pvc }
