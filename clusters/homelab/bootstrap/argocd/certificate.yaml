apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-server-tls
spec:
  secretName: argocd-server-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-dns
  dnsNames:
  - argocd.support4u.se
